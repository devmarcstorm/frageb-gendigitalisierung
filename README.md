# Fragebögendigitalisierung

> Virtuelle Entwicklungsumgebung erstellen
```zhs
python3 -m venv venv
. venv/bin/activate
```

> pre-commit-Konfiguration installieren (nur für Contributors)
```zhs
pip install pre-commit
pre-commit install
```

> Script ausführen
```zsh
python3 -m run
```
